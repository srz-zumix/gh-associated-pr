name: Test
on:
  pull_request:

env:
  GH_TOKEN: ${{ github.token }}

permissions: {}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - run: make install
      - run: |
          gh associated-pr ${{ github.ref }}
